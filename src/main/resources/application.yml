spring:
  application:
    name: issuemap
  datasource:
    url: jdbc:mysql://localhost:3306/issuemap
    username: root
    password: root1234!@
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    open-in-view: false
    hibernate:
      ddl-auto: update
  security:
    user:
      name: user
      password: user123
    enabled: false

