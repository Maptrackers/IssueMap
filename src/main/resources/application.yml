spring:
  application:
    name: issuemap
  datasource:
    url: jdbc:mysql://localhost:3306/issuemap
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    open-in-view: false
    hibernate:
      ddl-auto: update
